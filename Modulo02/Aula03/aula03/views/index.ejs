<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>

    <p>
      <%= customers.length %> clientes cadastrados
    </p>
    <table>
      <thead>
        <tr>
          <td>Nome</td>
          <td>Idade</td>
          <td>Regiões</td>
          <td>Ações</td>
        </tr>
      </thead>
      <tbody>
        <% 
            customers.forEach(item => { 
        %>
        <tr>
          <td><a href="/edit/<%= item._id %>"><%= item.nome %></a></td>
          <td><%= item.idade %></td>
          <td><%= item.cidade %></td>
          <td>
            <a href="/edit/<%= item._id %>">
              Edit
            </a>
            <a href="/delete/<%= item._id %>" onclick="return confirm('Você tem certeza que deseja excluir este cliente? Pensa bem, não tem volta')">
              Delete
            </a>
          </td>
        </tr>
        <% 
            }) 
        %>
      </tbody>
    </table>
    <hr>
    <a href="/new">Cadastrar Novo</a>
  </body>
</html>
